# M5.5 — RL extension (design-review milestone — do not implement yet)

See `docs/ROADMAP.md` Phase 5.5 for the full write-up (mapped out for
review, per your request — not a build task until reviewed/accepted).
Depends on M5.

- [ ] Circulate `docs/ROADMAP.md`'s Phase 5.5 section (+ design doc §6, §7,
      §7.4) for review
- [ ] Once reviewed/accepted: reshape M3's AUC metric into the per-step
      reward
- [ ] Add the value head (budget-aware branch/action comparison, replacing
      the hard incumbent/flat partition)
- [ ] Confirm the MDN policy head (M4) is in place — structurally required
      here, not optional
- [ ] Advantage-weighted regression as the first step (reweight the existing
      imitation loss), before attempting full on-policy policy-gradient
      training
- [ ] Re-measure against M5's own result and M6's baselines — the relevant
      comparison is "closes the budget-blindness/exploitation-pull gap
      measured in M5," not "beats EI" in isolation
