# M7 — Consolidate / port back the rest of `archive/`

See `docs/ROADMAP.md` Phase 7 for the *why*. Depends on M1–M6 being real,
working components, not scaffolds.

- [ ] Sweep `archive/` for anything else worth porting once M1–M6 are real,
      working components
- [ ] Fix issues in ported code rather than carrying them forward as-is
