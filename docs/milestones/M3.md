# M3 — Inc-AUC metric

See `docs/ROADMAP.md` Phase 3 for the *why*.

- [ ] Port `archive/src/prototype/l2o_rlsf/model/rewards/area_under_incumbent_curve.py`
      into `src/anytimeacquisition/` as a standalone metric (not reward-only)
- [ ] Support the per-step decomposition needed later for M5.5's reward
      (`r_t = log(incumbent_{t-1}) − log(incumbent_t)`), not just the
      whole-trajectory AUC number
- [ ] **Unit tests (required, not optional/manual-only)**: matches a
      hand-computed AUC on a small fixed trajectory; per-step decomposition
      telescopes to the whole-trajectory value; monotonicity under a
      strictly-improving trajectory; invariant to steps that don't improve
      the incumbent
- [ ] Small `if __name__ == "__main__":` demo: plot raw values vs. the
      incumbent curve for one trajectory (the prototype's `plot_reward`
      already does this — port it)
