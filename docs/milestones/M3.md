# M3 — Inc-AUC metric

See `docs/ROADMAP.md` Phase 3 for the *why*.

- [x] Port `archive/src/prototype/l2o_rlsf/model/rewards/area_under_incumbent_curve.py`
      into `src/anytimeacquisition/` as a standalone metric (not reward-only)
      — `src/anytimeacquisition/metrics/inc_auc.py`, no Hydra config group
      (same precedent as `utils/flatten.py` — this isn't a swappable
      strategy, just a plain importable module)
- [x] Support the per-step decomposition needed later for M5.5's reward
      (`r_t = log(incumbent_{t-1}) − log(incumbent_t)`), not just the
      whole-trajectory AUC number — `log_incumbent_stepwise_reward`, kept
      deliberately separate from `log_incumbent_auc` (the two are related
      but not the same quantity; see the module docstring)
- [x] **Unit tests**: `tests/test_inc_auc.py` — hand-computed AUC on a fixed
      trajectory; stepwise reward telescopes to total log-incumbent
      improvement; strictly positive under a strictly-improving trajectory;
      AUC is lower for a dominating trajectory; stepwise reward is exactly
      zero on non-improving steps
- [x] Small `if __name__ == "__main__":` demo: `plot_incumbent_curve`,
      ported from the prototype's `plot_reward`
