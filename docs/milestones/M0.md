# M0 — Scaffolding

Done, on `claude-init`. See `docs/ROADMAP.md` for the full plan this
milestone sets up.

- [x] `claude-init` branched, previous implementation moved to `archive/`
- [x] `pyproject.toml` via `uv` (package `anytimeacquisition`, src layout)
- [x] Hydra config groups scaffolded: `benchmarks`, `priors`,
      `models/{surrogates,acquisition,baselines}`, `trainer`, `callbacks`,
      `deployment`
- [x] End-to-end dummy pipeline proves Hydra compose + instantiate works
      (`tests/test_hydra_config.py`)
- [x] Run provenance (clean-commit + overrides enforcement, MLflow tags)
      (`tests/test_provenance.py`)
- [x] MLflow file-store backend wired (no SQLite/Postgres)
- [x] `scripts/submit.sh` + `scripts/slurm/train.sbatch` (single run + multirun
      via `hydra-submitit-launcher`)
- [x] Research roadmap/milestones written
- [ ] Resolve `docs/OPEN_QUESTIONS.md` items blocking M1
