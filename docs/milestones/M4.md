# M4 — Model: PFN + ActionHead (VLA integration)

See `docs/ROADMAP.md` Phase 4 for the *why*. Depends on M2.

- [x] ~~Port~~ `archive/.../repo/src/model/action_head.py` — **could not be
      ported**: it didn't survive the accidental `rm -rf` this repo was
      recovered from (never read into the session the recovery replayed, so
      no copy exists anywhere in this repo's history). Built fresh instead,
      as `src/anytimeacquisition/models/action_head.py`, against the design
      doc + the surviving `archive/src/exit/model/asdf.py` fragment, cited
      to pi0/pi0.5 (Physical Intelligence) for the multi-layer,
      expert-specific-weights idea — see the module docstring for exactly
      what's borrowed vs. deliberately different (this PFN is genuinely
      frozen, unlike pi0.5's backbone; no flow matching). Cross-attention
      into the frozen PFN's multi-layer train-token hidden states,
      gradients blocked into the PFN — verified numerically
      (`tests/test_action_head.py::test_pfn_gradients_are_none_after_action_head_backward`,
      plus the `__main__` demo against a real checkpoint)
- [x] Explicit auxiliary tokens: step count, remaining budget, incumbent
      value, recent-improvement trend — four separate tokens
      (`AUX_FEATURE_NAMES` in `action_head.py`)
- [x] Policy head + value head — **provisional single-Beta-per-dimension
      placeholder only**, see below; not the final head
- [ ] **Run the multimodality diagnostic before choosing the policy head
      distribution** (design doc §4.2/§8 stage 1): fixed states, exploit
      search with several random seeds, check target spread. Determines
      which branch of the policy-head decision below applies. **Deliberately
      not run yet** (explicit user call, 2026-08-28) — needs a trained
      ActionHead + rollout infrastructure this pass didn't build
- [ ] **Policy head, gated on the multimodality diagnostic above (decided
      2026-08-27):**
  - Tight clustering (no real multimodality) → a **single Beta distribution
    per dimension** (not plain point-regression). Native `[0, 1]` support
    matches `x`'s domain exactly (no Gaussian+squash artifacts), and it's
    the distributional head M5.5 needs anyway for a differentiable
    `log π(x)` — strictly better than point-regression even without
    multimodality.
  - Genuinely separated clusters → a **mixture of per-dimension Beta
    products** (mixture-of-Beta instead of the usual mixture-of-diagonal-
    Gaussian MDN) — same bounded-support benefit, plus multimodality via the
    mixture. Standard MDN practice already accepts the per-component
    diagonal/factorized approximation (correlation gets captured via the
    mixture itself, not within a single component) — same tradeoff a
    factorized bar distribution would have paid, without that option's
    combinatorial binning cost.
  - **Either variant needs the `other_diff` prototype's documented Beta
    reparameterization fix**: `archive/src/prototype/other_diff/README.md`
    found real NaN-gradient instability when α or β drop below 1.0 during
    training (the Beta becomes U-shaped, spikes at the boundaries) — clamp
    via `softplus(x) + 1.0`, not a bare `softplus`.
  - Rejected regardless of diagnostic outcome: factorized bar distribution
    over `x`'s dims (combinatorially worse than Beta for the same
    independence-assumption cost), full flow matching (disproportionate
    machinery for a single-oracle deterministic target, not genuine
    multi-demonstrator multimodality).
  - Make sure whichever variant is built supports evaluating the policy
    density π(x | context) at arbitrary query points on a grid, not just
    sampling — needed for M5's interpretability diagnostic
- [ ] **VLA-bridge demo, real signal check**: the prototype's "blind
      ActionHead" ablation (PFN hidden states zeroed, aux features
      untouched) vs. the real version, showing the real version
      fits/predicts meaningfully better. Needs actual fitting, not just a
      forward pass — **not done this pass**, out of scope for a smoke test
      (explicit user ask, 2026-08-28: "do a smoke fwd test", not training)
- [x] Tests: forward pass runs at target shapes
      (`test_forward_pass_runs_at_target_shapes`); PFN-gradient-isolation
      check (`test_pfn_gradients_are_none_after_action_head_backward`);
      plus a batch-independence check not originally listed here
      (`test_action_head_output_is_batch_independent`)
- [ ] Memorization/ablation sanity check in the spirit of the prototype's
      "blind ActionHead" comparison — same as the VLA-bridge demo above,
      needs real training, not done this pass
