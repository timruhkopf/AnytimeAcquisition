# M6 — Baselines (medium-dim, not low-dim, not high-dim)

See `docs/ROADMAP.md` Phase 6 for the *why*. Independent of M1–M5 to build,
but needed as the comparison point for M5's evaluation.

- [ ] Pick a BO library for GP + EI/UCB/PI/ES (open — see
      `docs/OPEN_QUESTIONS.md`)
- [ ] Land in `src/anytimeacquisition/models/baselines/` (currently
      `configs/models/baselines/TODO.md` placeholder), one config per
      acquisition function
- [ ] Same medium-dim benchmark instances M5 evaluates against
- [ ] Tests: each baseline actually improves over random search on a known
      easy instance (sanity check, not a research claim)
